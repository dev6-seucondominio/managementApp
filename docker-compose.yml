web:
  build: .
  command: bundle exec rails s -p 8000 -b '0.0.0.0'
  ports:
    - "8000:8000"
  links:
    - db
  volumes:
    - .:/erp
  volumes_from:
    - bundle
bundle:
  image: managementapp_web
  volumes:
    - /bundle
db:
  image: postgres
